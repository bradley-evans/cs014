/*
Trees.

Recursive Definition:
A tree is a collection of nodes where one node is considered the root.
A tree has "N" nodes and "N-1" edges. Each edge connects some node to its parent
and every node except the root has exactly one parent.